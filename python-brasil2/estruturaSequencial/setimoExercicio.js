/**
 * Faça um programa que calcule a área de um quadrado, em seguida mostre
 * o dobro desta área para o usuário.
 */

function area(lado1, lado2) {
  return 'O dobro da área do quadrado equivale a: ' + (lado1 * lado2) * 2;
}
