/**
 * Faça um programa, com uma função que necessite de um argumento.
 * A função retorn o valor de caractere 'P', se seu argumento for
 * positivo, e 'N', se seu argumento for zero ou negativo.
 */

function number(n) {
  return n > 0 ? 'P' : 'N';
}
